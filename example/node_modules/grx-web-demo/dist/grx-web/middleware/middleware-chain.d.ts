import { Ctx } from '../types';
import { Middleware } from './middleware';
declare const middlewares: Middleware[];
export declare function use(middleware: Middleware): void;
export declare function next(ctx: Ctx, index?: number): Promise<void>;
export { middlewares };
